import React from "react";
// TODO: Replace with a real scenario list filtered by workflowId
const WorkflowScenarios = ({ workflowId }: { workflowId: string }) => (
  <div>Test Scenarios for workflow: {workflowId} (implement scenario list here)</div>
);
export default WorkflowScenarios; 